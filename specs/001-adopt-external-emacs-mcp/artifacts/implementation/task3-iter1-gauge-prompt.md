# Gauge Code Review — Task 3 Iteration 1

## Task
Create `gemini-cli-ide-tools.el` that registers
`gemini-cli-ide-mcp-get-terminal-input` into the external `emacs-mcp`
registry. Per FR-2 / FR-12 / C-9.

## Inputs to read
1. The new file:
   `/Users/ezchi/Projects/gemini-cli-ide/gemini-cli-ide-tools.el`
2. Forge artifact:
   `/Users/ezchi/Projects/gemini-cli-ide/specs/001-adopt-external-emacs-mcp/artifacts/implementation/task3-iter1-forge.md`
3. Spec / clarifications:
   `specs/001-adopt-external-emacs-mcp/spec.md` FR-2, FR-12;
   `clarifications.md` C-8, C-9.
4. Constitution coding standards:
   `.steel/constitution.md` §"Coding Standards".
5. emacs-mcp source for the symbols this file uses:
   - `~/Projects/emacs-mcp/emacs-mcp-tools.el` (the `emacs-mcp-deftool` macro)
   - `~/Projects/emacs-mcp/emacs-mcp-protocol.el` (for `emacs-mcp--current-session-id` semantics)
   - `~/Projects/emacs-mcp/emacs-mcp-session.el` (for `emacs-mcp--session-get` and `emacs-mcp-session-project-dir`)
   - `~/Projects/emacs-mcp/README.org` "Adding Custom Tools" section.

## Verify

1. **Correctness.**
   - The MCP tool name string is exactly
     `gemini-cli-ide-mcp-get-terminal-input` (FR-12 verbatim
     preservation).
   - The tool description string matches the original
     `gemini-cli-ide-emacs-tools.el:369` description verbatim.
   - The handler returns a non-empty string in every code path
     (no `nil`, no unhandled error).
   - The session-context lookup uses `emacs-mcp--current-session-id`
     correctly: it must check that the dynamic var is non-nil
     before dereferencing it (the var is `nil` outside an active
     request, per `emacs-mcp-tools.el:23`).

2. **Namespace / coding standards.**
   - `lexical-binding: t` cookie present.
   - File ends with `(provide 'gemini-cli-ide-tools)` and the
     standard ends-here comment.
   - Public symbol: `gemini-cli-ide-tools--current-project-dir`
     uses the `--` prefix for private (correct).
   - The tool handler is registered via `emacs-mcp-deftool` which
     itself manufactures a `emacs-mcp-tool-NAME--handler` symbol
     — that symbol is generated by the macro and is therefore not
     a namespacing violation by us.
   - Docstring on `gemini-cli-ide-tools--current-project-dir`.

3. **Constitutional alignment.**
   - Principle 7 (no leakage of buffer contents / paths in logs):
     the handler returns the user's typed input by design (this is
     the tool's contract — Gemini asked for it), but does not log
     it.
   - Principle 5 (no network calls): confirmed — only local Emacs
     buffer access.
   - "One concern per file": file does one thing.

4. **Deviation honesty.**
   - The forge artifact admits the plan said "move
     gemini-cli-ide--get-terminal-input verbatim" but the helper
     was already in `gemini-cli-ide.el`. Confirm by reading
     `/Users/ezchi/Projects/gemini-cli-ide/gemini-cli-ide.el`
     line 1227 that this is true. If it is, the deviation is
     acceptable.

5. **Forward-declaration correctness.**
   - `(declare-function gemini-cli-ide--get-terminal-input
     "gemini-cli-ide" (buffer))` matches the actual signature.
   - `(defvar gemini-cli-ide-buffer-name-function)` for the
     defcustom — correct pattern when used as a function value.

6. **Build cleanliness.**
   - The forge artifact reports zero warnings from
     `batch-byte-compile`. Re-run mentally: any obvious cause for
     a warning the artifact missed?

## Severity rubric
- BLOCKING: incorrect tool name / description (would break FR-12),
  unhandled `nil` from `emacs-mcp--current-session-id`, namespace
  violation, file fails to byte-compile.
- WARNING: stylistic deviation from constitution coding standards,
  brittle reliance on private upstream symbol with no comment.
- NOTE: nice-to-have polish.

## Output format

```markdown
# Gauge Review — Task 3 Iteration 1

## Summary
(1–3 sentences.)

## Correctness
(...)

## Coding Standards
(...)

## Constitutional Alignment
(...)

## Issues

### BLOCKING
(...)

### WARNING
(...)

### NOTE
(...)

VERDICT: APPROVE
```

OR

```markdown
... (same structure) ...

VERDICT: REVISE
```

## Hard rules
- Be strict.
- End with exactly `VERDICT: APPROVE` or `VERDICT: REVISE`.
- Do NOT modify any files.
