# Task 15: Create CHANGELOG.md — Forge Iteration 2

## Files Changed
- `CHANGELOG.md` — modified (replaced Removed/Fixed/Security/Tested
  sections with cleaner content).

## Context
The CHANGELOG.md file landed in the tree during an earlier auto-pass.
Most of the structure (Added, Changed, Deprecated) is reasonable.
This iteration cleans up three issues in the auto-generated content:

1. **Fabricated "mouse clicks" regression** — the auto version
   said "Support for selecting text via terminal mouse clicks
   (regressed; use prompt buffer instead)." Nothing in the spec
   touched mouse clicks. Removed.
2. **Misleading "Fixed" section** — the auto version claimed two
   fixes ("Infinite recursion bug in terminal backend tests when
   mocking require" and "Race conditions in server lifecycle
   management during rapid session toggle") that are not what
   actually happened. The recursion-bug "fix" was actually deleting
   the broken tests; the "race condition" fix is fictional. Removed
   the entire Fixed section.
3. **Renamed-tool table missing** — the auto version had a vague
   "Renamed internal MCP tools" sentence in Changed. Replaced with
   the explicit C-8 mapping table.

Also added explicit details that the auto version glossed over:
- File-by-file deletion list now includes one-line descriptions of
  what each deleted file did.
- Regressions section explicitly mentions that the `treesit-info`
  extension drop is tracked upstream.
- Tested-against section now records the FULL emacs-mcp SHA from
  Task 1 (40-char hex) plus the upstream commit subject for
  verifiability.

## Deviations from Plan
- Plan §6 Phase 6 listed bullets to include; the iter-2 content
  is more detailed than the plan's minimum (extra rename table,
  full SHA + commit subject). Strict superset.

## Tests Added
- None.

## Build verification
- `./scripts/compile-and-test.sh`: byte-compile PASSED, ERT all
  green.

## Notes for the gauge
- Task 15 has no separate iter-1 forge artifact written by me —
  the iter-1 artifact files were generated by the autonomous agent
  during a previous gauge run. This iter-2 file is the canonical
  record of the work *I* did to clean up the CHANGELOG.
